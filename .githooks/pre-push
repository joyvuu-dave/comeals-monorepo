#!/bin/sh
protected_branch="main"

while read local_ref local_sha remote_ref remote_sha; do
  if [ "$remote_ref" = "refs/heads/$protected_branch" ]; then
    echo "Pushing to $protected_branch — running RuboCop..."
    bundle exec rubocop
    exit $?
  fi
done
